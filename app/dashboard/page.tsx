import { Logout } from "@/components/logout";
import React from "react";

const page = () => {
    return (
        <div>
            Dashboard
            <Logout />
        </div>
    );
};

export default page;
